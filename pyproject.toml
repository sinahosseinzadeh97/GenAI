[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "querymind"
version = "0.1.0"
description = "QueryMind MCP Server: Natural Language to SQL"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.2.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "aiosqlite>=0.20.0",
    "asyncpg>=0.29.0",
    "fastapi>=0.111.0",
    "uvicorn>=0.30.0",
    "sse-starlette>=2.1.0",
    "httpx>=0.27.0",
    "pypdf>=4.0.0",
    "boto3>=1.34.0",
    "pgvector>=0.3.0",
    "python-multipart>=0.0.9",
    "openai>=1.0.0",
    "redis>=5.0.0",
    "alembic>=1.13.0",
    "slowapi>=0.1.9"
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0"
]
